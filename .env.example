# Leave this as /api when the frontend is served by the same deployment as
# the FastAPI agent backend. For a separately hosted frontend, use the complete
# public backend URL, including /api.
VITE_API_BASE_URL=/api
